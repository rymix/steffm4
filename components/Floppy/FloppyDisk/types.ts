export type StyledFloppyProps = { $floppyColor: string };

export type StyledTopProps = { $floppyColor: string };

export type StyledSliderProps = { $sliderColor: string; $hovered: boolean };

export type StyledDownProps = {
  $labelColor: string;
  $labelSecondColor: string;
};

export type StyledNotesProps = {
  $textColor: string;
  $font: string;
  $fontSize: number;
  $rotation: number;
  $fontSizeMobile: number;
};
